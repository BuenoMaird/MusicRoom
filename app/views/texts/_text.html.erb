<li class="<%=  self_or_other(text) %>">
  <div class="avatar">
    <img src="http://placehold.it/50x50" />
  </div>
  <div class="chatboxtextcontent">
    <p><%= text.body %></p>
    <time datetime="<%= text.created_at %>" title="<%= text.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= text_interlocutor(text).name %> • <%= text.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>